# PR 36114 — test(voice): add local continuous voice smoke harness

## Objective

Verify PR is still relevant, fix all issues, and get checks green.

## Tasks

- [ ] Review PR description, files changed, and current CI status (`gh pr view 36114`, `gh pr checks 36114`).
- [ ] Identify failing checks / review comments and root cause.
- [ ] Fix issues with minimal, scoped changes.
- [ ] Run validation:
  - `pnpm lint` (if too heavy, note and run targeted lint)
  - `pnpm test:voice:continuous:local` (note any hardware limits)
- [ ] Update PR with evidence (commands run, results).

## Validation Commands

- pnpm lint
- pnpm test:voice:continuous:local

## Completion Summary

- Status:
- Files changed:
- Validation results:
- Evidence:
